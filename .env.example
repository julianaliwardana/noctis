# Consumed only by docker-compose.yml (Postgres container).
# App secrets live in apps/api/.env (see apps/api/.env.example);
# web vars in apps/web/.env.local (see apps/web/.env.example).
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=noctis
